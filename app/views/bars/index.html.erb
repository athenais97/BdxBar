<p id="notice"><%= notice %></p>

<h1>Bars de Bordeaux</h1>

<div class="tbl-header">
    <table cellpadding="0" cellspacing="0" border="0">
      <thead>
        <tr>
          <th>Nom</th>
          <th>Genre</th>
          <th>Description</th>
          <th>Modifications</th>
        </tr>
      </thead>
    </table>
  </div>
  <div class="tbl-content">
    <table cellpadding="0" cellspacing="0" border="0">
  <tbody>
    <% @bars.each do |bar| %>
    </div>
      <tr>
        <td><%= bar.nom %></td>
        <td><%= bar.genre %></td>
        <td><%= bar.description %></td>
        <td><%= link_to 'Show', bar %></td>
        <td><%= link_to 'Edit', edit_bar_path(bar) %></td>
        <td><%= link_to 'Destroy', bar, method: :delete, data: { confirm: 'Etes-vous sûr?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'New Bar', new_bar_path %>