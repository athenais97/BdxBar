<p id="notice"><%= notice %></p>

<h1>Bars de Bordeaux</h1>
<h2>Ajoutez votre liste des bars de Bordeaux!</h2>
<table>
<div class="genre">
  <thead>
    <tr>
      <th>Genre</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  </div>
  <tbody>
    <% @bars.each do |bar| %>
      <tr>
        <td><%= bar.genre %></td>
        <td><%= link_to 'Show', bar %></td>
        <td><%= link_to 'Edit', edit_bar_path(bar) %></td>
        <td><%= link_to 'Destroy', bar, method: :delete, data: { confirm: 'Etes-vous sûr?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<button class="button1">Nouveau Bar
<%= link_to 'New Bar', new_bar_path %>
</button>
